# This exercises a mixed Go/LLVM GoObj link. The Python driver skips when a
# suitable Go linux/amd64 toolchain is unavailable.

# RUN: %if x86-registered-target %{ %python %S/Inputs/goobj-link-go-toolchain.py --llc llc --work-dir %t.dir %}
